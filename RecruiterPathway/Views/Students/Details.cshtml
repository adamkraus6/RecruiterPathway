@model RecruiterPathway.ViewModels.StudentViewModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Student</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Student.FirstName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Student.FirstName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Student.LastName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Student.LastName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Student.Degree)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Student.Degree)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Student.GradDate)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Student.GradDate)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Student.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
    <br />
    <a asp-action="AddComment" asp-route-id="@Model.Student.Id">Add Comment</a> |
    <a asp-action="AddToWatchList" asp-route-id="@Model.Student.Id">Add To Watchlist</a> |
    <a asp-action="AddPipelineStatus" asp-route-id="@Model.Student.Id">Set Pipeline Status</a>
</div>
<br />
<table class="table">
    <thead>
        <tr>
            <th>
                Commented At:
            </th>
            <th>
                Comment Left By:
            </th>
            <th>
                Comment:
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Student.Comments)
        {
            <tr>
                <td>
                    @{
                        var date = item.Time.ToShortDateString() + " at " + item.Time.ToShortTimeString();
                    }
                    @Html.DisplayFor(modelItem => date)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Recruiter.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ActualComment)
                </td>
            </tr>
        }
    </tbody>
</table>
